class Cat {

   constructor( name, age, color, breed,){
      this.name= name;
      this.age = age;
       this.color=color;
       this.breed=breed
    }
// Methods

    getDescription() {
       return `name ${this.name},age ${this.age},color${this.color},breed${this.breed}`
    }

    setData(sname,scolor,sbreed) {
       sname=this.name
       scolor=this.color
       sbreed=this.breed
          }
 
       setdisplay(){
           console.log(this.Jo,this.Sammy)
       }
}

      const Sammy= new Cat('Sammy',8,'Black','American')
      const Jo = new Cat('Jo',6,'White','persian')
    
     console.log(Sammy.getDescription());
     // console.log(Jo);

